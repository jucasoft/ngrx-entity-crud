# .github/workflows/pr-checks.yml
# Questo workflow agisce come guardiano per il branch master.
name: Pull Request Checks

on:
  pull_request:
    branches:
      - 'master'

jobs:
  validate:
    name: Validate Pull Request
    uses: ./.github/workflows/reusable-build.yml
