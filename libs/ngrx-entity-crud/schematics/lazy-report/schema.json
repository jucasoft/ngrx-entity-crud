{
  "$schema": "http://json-schema.org/schema",
  "$id": "LazyReport",
  "title": "Lazy store report",
  "type": "object",
  "properties": {
    "output": {
      "description": "Percorso del file di report da generare (relativo alla root del workspace). Stringa vuota = solo console.",
      "type": "string",
      "default": "lazy-report.md"
    },
    "format": {
      "description": "Formato del report scritto su file.",
      "type": "string",
      "enum": ["md", "json"],
      "default": "md"
    },
    "infraStores": {
      "description": "Store infrastrutturali da escludere dai candidati lazy (nome cartella).",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": ["router-store"]
    }
  },
  "required": []
}
