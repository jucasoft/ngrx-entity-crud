# graphify: aggiorna graphify-out/ al cambio branch (solo AST, non blocca mai).
# $1 = HEAD precedente, $2 = nuovo HEAD, $3 = 1 se è un cambio di branch.
[ "$3" = "1" ] && [ "$1" != "$2" ] && sh scripts/graphify-hook.sh "$1" "$2"
exit 0
